package com.sportsync.backend.model.admin;

/** Medio de pago elegido por el usuario (el procesamiento real pasa por el proveedor). */
public enum MetodoPago {
    MERCADO_PAGO,
    TARJETA_CREDITO,
    TARJETA_DEBITO
}
