package com.sportsync.backend.model.admin;

/** Estados de una operación de pago. */
public enum EstadoPago {
    PENDIENTE,
    APROBADO,
    RECHAZADO,
    CANCELADO,
    REEMBOLSADO,
    VENCIDO
}
